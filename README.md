# nvim

neovim configruation writing by lua
suitable rust go lua markdown

## font

- [x] `operator & font-meslo-lg-nerd-font` osx

## required

luafmt `npm install -g luafmt`
prettier `npm install --save-dev --save-exact prettier`
